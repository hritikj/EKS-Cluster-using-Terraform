apiVersion: v1
kind: Service
metadata:
  name: html-css-game-service
spec:
  type: LoadBalancer

  selector:
    app: game   # must match Deployment labels

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
